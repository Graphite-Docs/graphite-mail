import React from 'reactn';

export default class Dimmer extends React.Component {
    render() {
        return(
            <div style={{display: "none"}} id="dimmer" className="dimmer">
            </div>
        )
    }
}